import { useState } from 'react';
import { UserPlus } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export default function Register() { const { register } = useAuth(); const navigate = useNavigate(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirmation, setConfirmation] = useState(''); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); setError(''); if (password !== confirmation) { setError('As senhas não coincidem.'); return; } try { await register(email, password); navigate('/', { replace: true }); } catch (err) { setError(err.message); } }; return <main className="screen" style={{ display: 'grid', placeItems: 'center' }}><form className="watch-safe" style={card} onSubmit={submit}><div style={{ fontSize: '3rem' }}>🏹</div><h1>Criar conta</h1><p>Cadastre seu e-mail para salvar suas sessões e configurações.</p><label>E-mail<input type="email" autoComplete="email" required value={email} onChange={(e) => setEmail(e.target.value)} /></label><label>Senha<input type="password" autoComplete="new-password" minLength="8" required value={password} onChange={(e) => setPassword(e.target.value)} /><small>Mínimo de 8 caracteres.</small></label><label>Confirmar senha<input type="password" autoComplete="new-password" required value={confirmation} onChange={(e) => setConfirmation(e.target.value)} /></label><button type="submit" style={button}><UserPlus size={18} /> Criar conta</button>{error && <p role="alert" style={{ color: '#f87171' }}>{error}</p>}<Link to="/login" style={{ color: '#fbbf24', textAlign: 'center' }}>Já tenho uma conta</Link></form></main>; }
const card = { maxWidth: '26rem', padding: '2rem', border: '1px solid #3f3f46', borderRadius: '1.5rem', background: '#18181b', display: 'grid', gap: '1rem' }; const button = { width: '100%', minHeight: '3.25rem', border: 0, borderRadius: '999px', background: '#f59e0b', fontWeight: 700, display: 'flex', gap: '.5rem', alignItems: 'center', justifyContent: 'center' };
