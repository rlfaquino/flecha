import { useState } from 'react';
import { LogIn, UserPlus } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export default function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); setError(''); try { await login(email, password); navigate('/', { replace: true }); } catch (err) { setError(err.message); } }; return <main className="screen" style={{ display: 'grid', placeItems: 'center' }}><form className="watch-safe" style={card} onSubmit={submit}><div style={{ fontSize: '3rem' }}>🏹</div><h1>Entrar</h1><p>Use seu e-mail e senha para acessar suas configurações.</p><label>E-mail<input type="email" autoComplete="email" required value={email} onChange={(e) => setEmail(e.target.value)} /></label><label>Senha<input type="password" autoComplete="current-password" required value={password} onChange={(e) => setPassword(e.target.value)} /></label><button type="submit" style={button}><LogIn size={18} /> Entrar</button>{error && <p role="alert" style={{ color: '#f87171' }}>{error}</p>}<Link to="/forgot-password" style={{ color: '#fbbf24', textAlign: 'center' }}>Esqueci minha senha</Link><Link to="/register" style={{ color: '#fbbf24', display: 'flex', gap: '.4rem', justifyContent: 'center', alignItems: 'center' }}><UserPlus size={17} /> Criar uma conta</Link></form></main>; }
const card = { maxWidth: '26rem', padding: '2rem', border: '1px solid #3f3f46', borderRadius: '1.5rem', background: '#18181b', textAlign: 'center' }; const button = { width: '100%', minHeight: '3.25rem', border: 0, borderRadius: '999px', background: '#f59e0b', fontWeight: 700, display: 'flex', gap: '.5rem', alignItems: 'center', justifyContent: 'center' };
